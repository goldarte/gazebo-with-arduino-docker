# gazebo-with-arduino

Docker image that contains gazebo classic 11 + arduino-cli. Suitable for [flix](https://github.com/okalachev/flix) flight controller simulation.

## Usage

- `build.sh` - build image locally
- `run.sh` - example for run container and attach to bash
